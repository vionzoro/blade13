# BLADE:13 — PRISM BREAK — 交接文档

**给接手者（Codex）**：本文件包含接管这个项目所需的全部上下文。
最后更新：2026-08-14 · 主题版目标 ≤ 13,100 字节；最终大小以 build.py 最新输出为准

---

# 第一部分 · 比赛信息

## 1.1 基本盘

| 项 | 内容 |
|---|---|
| 赛事 | js13kGames 2026（第 15 届） |
| 官网 | https://js13kgames.com/2026/ |
| 赛期 | 2026-08-13 开赛 → 2026-09-13 截止（整月） |
| 硬限制 | zip 包 **≤ 13,312 字节**（13×1024） |
| 参赛资格 | 全球开放，无年龄/职业限制 |
| 参赛者 | Vion / Namek Game Studio（个人参赛，非学生） |

## 1.2 规则要点（已逐条核实）

1. **全部资源必须在 zip 内**。禁止任何外部请求——CDN、字体、图片、音频文件、统计脚本一律不行。依赖外部资源会被排除出总榜。
   - 本项目的 `build.py` 内置红线检查：源码里出现 `fetch` / `XMLHttpRequest` / `importScripts` 直接构建失败。
2. **必须提交两份来源**：① zip 包；② **公开 GitHub 仓库**，内含可读、未混淆、**能真正构建出游戏**的完整源码（不是把 zip 解压一遍）。
3. **主题是 Unicorns and Rainbows**。本作的诠释已落地：UNICORN 是城市的彩虹能源 AI，它把机械腐化成棱镜兽；玩家收集七色碎片、触发 PRISM BREAK，并在 BLACK UNICORN 结束本局前切断网络。
4. **提交表单在开赛后数日才开放**（2025 年约为 8 月 18 日）。
5. 游戏至少要能在一个浏览器跑，支持越多越好。**控制台报错会扣分；跑不起来直接不予接受。**

## 1.3 评分与分类

- **六项评分**：Innovation / Theme / Gameplay / Graphics / Audio / Controls
- **无音频的作品，Audio 项直接零分**
- **Controls 按 Desktop / Mobile / WebXR 分开打分**
- **两个基础分类：Desktop 与 Mobile。提交时两个都要勾。**
  - 关键规则：如果投票者玩了一个双端作品却只给 Desktop 操控打分，官方视为"没在手机上试过"，这一票**不计入移动端排名**
  - 战略含义：**移动榜投票样本远少于总榜**，认真做触屏的人是少数 → 这是本项目胜率最高的赛道。提交页面必须显式写明「Playable on mobile」，不主动喊就没人试
- 投票主力是**其他参赛者**（多为桌面浏览器），另有专家评委，每人随机分配 20-30 个作品

## 1.4 本项目的名次判断（交接时的评估）

| 维度 | 判断 |
|---|---|
| Audio / Graphics / Controls | 强项 |
| Gameplay | 中上 |
| **Innovation** | **弱项** —— 幸存者类在近几届 jam 泛滥，评委有品类疲劳 |
| **Theme** | **已完成** —— UNICORN AI、棱镜兽、七色碎片、BLACK UNICORN 与 PRISM BREAK 共同支撑 Unicorns and Rainbows 主题 |

- 总榜前 13：约 5-10%
- 移动分类前列：约 15-25%
- 进 Top 100：80%+

**提名次的非技术杠杆**（成本极低，容易被忽略）：早交（投票期被玩到次数更多）、去玩别人的作品并认真留评论、作品页第一张 GIF 的质量、写 devlog。

## 1.5 原创与 IP 红线（**务必遵守**）

- 提交材料、游戏内文本、仓库和页面描述不得出现第三方作品、角色、地点、技能或品牌专名。
- 机制描述使用通用类型语言；角色造型、命名、文案、代码、画面与声音均保持原创表达。
- 游戏现用的 BLINK / PRISM BREAK / 单分子刃 / CHROME / PROGRAM / OVERLOAD 均为项目自己的通用科幻术语体系。
- 发布前运行 `node tools/release-guard.mjs`，任何命中都必须清理后再打包。

---

# 第二部分 · 项目现状

## 2.1 交付物

| 文件 | 用途 |
|---|---|
| `BLADE13-js13k-submission.zip` | **提交包**，12,937 字节，SHA-256 `2e21127fb463fadf6e2afebb1f688d2152ca9da6e82e8afce3be1ef7388d0850`，内含单个 index.html |
| `BLADE13-submission-index.html` | 上面 zip 解开后的内容，可单独部署 |
| `BLADE13-playtest-dev.html` | 未压缩可读版，调试用，双击即玩 |
| 当前 Git 仓库 / worktree | **完整可读源码**，含 `src/`、`build.py`、`tools/`、文档与锁定依赖；公开源码时直接发布当前仓库 |
| `README.md` | 仓库 README，规则要求 |
| `提交清单.md` | 表单逐项填写内容 + 提交前自检表 |

## 2.2 源码结构（构建按文件名字典序合并）

| 文件 | 职责 |
|---|---|
| `src/00-core.js` | 常量、xorshift32 PRNG、坐标哈希、地形查询、玩家状态、reset() |
| `src/10-cards.js` | 19 张卡池、前置条件、抽卡逻辑 |
| `src/20-game.js` | 战斗、刷怪、威胁等级、闪现、PRISM BREAK、升级、结局 |
| `src/30-juice.js` | 打击停顿、冲击环、尸块、血渍、连杀、屏幕反馈 |
| `src/40-render.js` | 世界与 UI 渲染、HUD、卡牌界面、8×8 像素图标 |
| `src/45-art.js` | 角色与怪物作画 |
| `src/50-audio.js` | 合成器、音效表、程序化配乐 |
| `src/60-main.js` | 布局、输入、像素缓冲合成、主循环、自适应画质 |

## 2.3 构建

```bash
npm ci                 # 按 lockfile 安装固定版本
python3 build.py       # 合并 -> 红线检查 -> terser -> roadroller -> zip -> 报字节
```

产物在 `build/`：`BLADE13-dev.html`（可读）、`dist/index.html` + `BLADE13.zip`（提交包）。
`build.py` 每次输出**剩余字节数**，超限直接非零退出。

## 2.4 测试（改任何东西前后都要跑）

```bash
sh tools/qa.sh         # 快速验证：渲染、节奏、疾行者、IP、反作弊与打包回归
npm run verify         # 完整发布验证：再覆盖专项探针、种子扫描、双构建哈希
```

| 层 | 覆盖 |
|---|---|
| `tools/bot-test.mjs` | 模拟核心：确定性、五分钟自动对局、数值不爆炸、卡池层数上限 |
| `tools/render-smoke.mjs` | 全部渲染路径（Canvas 桩 Proxy 化），四种状态 + 输入路径 |
| `tools/packed-smoke.mjs` | Roadroller 打包版自解压、启动主循环，绘制精确主题标题/目标并走完输入路径 |
| `tools/runner-test.mjs` | 疾行者计时器为有限数，且会实际进入蓄力暴冲 |
| `tools/pace-test.mjs` | 锁定开局密度、敌人解锁、22 秒威胁等级与 32 秒首领 |
| `tools/release-guard.mjs` | 公开文本发布前专名扫描，命中即失败 |

专项探针：

| 脚本 | 用途 |
|---|---|
| `tools/cheese.mjs` | **作弊流压测** —— 吸血坦克流 / 纯逃跑流 / 原地硬苟流 / 正常打法，四个机器人各堆一套苟活组合。**改任何数值后必跑** |
| `tools/bench.mjs` | 会走位的机器人（贴攻击距离边缘绕圈），用于难度调参 |
| `tools/terrain.mjs` | 地刺密度、伤害、出生点安全 |
| `tools/pose.mjs` | 拦截 Canvas 曲线指令，验证刀身与围巾的方向 |
| `tools/port.mjs` | 竖屏重排、触屏识别、轻点释放技能 |
| `tools/perf.mjs` | 满屏时每帧绘制调用统计 |
| `tools/seed-sweep.mjs` | 确定性种子扫描，复现间歇性崩溃 |

**注意**：`render-smoke` 与 `packed-smoke` 沙箱内的 `Math.random` 已替换为 LCG，测试可复现。

---

# 第三部分 · 玩法与数值现状

## 3.1 核心循环

站立 Q 版独角兽用单刀自动挥砍（扇形横扫，同时命中范围内所有敌人）→ 击杀掉经验 → 升级三选一 → 变强 → 怪更多更强。存活时间即分数。单刀是视觉收敛，不改变原攻击扇形、范围、伤害或命中次数。

英雄使用 `🦄` 式白色侧脸、金角、彩虹鬃毛，保留黄夹克、黑风衣、品红围巾与赛博义体。普通挥刀、BLINK 和 PRISM BREAK 均带彩虹反馈，强度依次增加；受伤和危险预警仍保持红色。

**三个主动决策点**：
1. **BLINK**（空格 / 轻点）：连续闪击最近 N 个敌人，途中无敌 0.35 秒。**2 格充能**，按时间恢复；击杀加速恢复但封顶；每次后 **0.85 秒硬锁**任何加成绕不过；落地 **0.42 秒不能出刀**
2. **PRISM BREAK**（F / 左下按钮）：玩家向棱镜兽收集七色碎片，击杀攒满后**不自动触发**，由玩家掐时机。改名不改机制：5 秒内攻速×2、移速+30%、闪现硬锁减半，期间每次击杀延长 0.07 秒（上限 8 秒）
3. **升级三选一**：`PROGRAM`(青) / `CHROME`(琥珀) / `OVERLOAD`(红，**必有代价**)

## 3.2 卡池（19 张，一局约只拿到 31%）

- 普通卡上限 2-4 层（**故意压低，制造机会成本**）
- 超载卡 6 张：+55% 伤害/-30% 生命、+50% 攻速/-25% 距离、+12% 吸血/-20% 生命、+45% 伤害/+55% 充能时间、+65% 生命/-22% 攻速、荆棘/+25% 受伤
- 进阶卡 4 张，需前置：Apex Edge(需 he≥3)、Blur(需 sw≥3)、Execute(需 fo≥2，暴击 2.4→3.6)、Ghostwalk(需 pc≥2，+1 充能格)
- 卡池抽空后有无限层兜底卡 Blade Oil，保证每级都有回报
- **前 4 级保底一张进攻卡** —— 消除开局抽卡悬崖

## 3.3 敌人（7 种 + 精英 + 首领）

| # | 名称 | 特点 | 登场 |
|---|---|---|---|
| 0 | 爬行兽 | 基础 | 0s |
| 1 | 疾行者 | 周期蓄力后 3.1 倍速暴冲 | 4s |
| 2 | 重装兽 | 高血高伤 | 12s |
| 3 | 咒术体 | 远程；130s 后扇形三连射，弹速 320 | 29s |
| 4 | 分裂体 | 死后分裂成 2 只爬行兽 | 19s |
| 5 | 引爆体 | 死时炸开 100 半径，波及其他怪；**玩家闪现撞它可主动引爆** | 40s |
| 6 | 孵化巢 | 每 2.6 秒产 2 兵，**不杀它屏幕永远清不干净** | 32s |

- **精英**（20s 后，5-16%）：品红脉冲描边，2.4 倍血、1.22 倍速、1.35 倍伤，**必掉血包**
- **首领**（首次 32s，之后每 28s）：7 倍血，每 4.6 秒蓄力 1.05 秒后震地（150 半径、1.3 倍伤），**击杀必掉一张升级卡**，必掉血包
- 屏幕边缘有指示箭头指向场外的孵化巢与首领

## 3.4 关键数值（改动前务必理解为何是这个值）

```js
血量成长   k  = 1 + (T/64)^1.95 * 2.5        // 指数: 前松后紧
伤害成长   dm = 1 + (T/70)^1.6  * 1.2
移速成长   sp = 1 + clamp(T/105, 0, 2.7)     // 后期怪比玩家快 -> "跑"失效
刷怪间隔   max(.12, .68 - T/170)
每波数量   2 + floor(T/22)
刷怪方向   55% 落在玩家前进方向 ±1.35 弧度的锥形内   // 直线跑=自投罗网
威胁等级   每 22 秒 +1, 同时来一波 tier²+1 只的定向涌潮
升级曲线   nxt = 3 + lv*2 + lv²*0.44
吸血       每秒最多回 6% 最大生命（预算制）
开局       场上直接放 22 只怪
地刺       4.5% 地格, sin(T*1.5+hash*62) > .55 时弹出, 伤害 12 + T*0.1
```

## 3.5 当前难度实测（`tools/cheese.mjs`）

| 打法 | 存活 |
|---|---|
| 吸血坦克流 | 78 / 74 / 64 s |
| 纯逃跑流 | 31 / 35 / 28 s |
| 原地硬苟流 | 74 / 61 / 43 s |
| 正常打法 | 44 / 37 / 46 s |

真人显著强于机器人（机器人不会读首领红圈、不会规划卡组、不会用无敌帧穿怪）。

---

# 第四部分 · 血泪教训（**接手前必读，这些坑都踩过一次**）

## 4.1 工程纪律

1. **补丁锚点字符串必须带断言**。用 python 字符串替换改源码时，**两次**因为空格数不匹配导致替换静默失败（一次是坐标哈希函数没插进去、一次是敌人数据行没插进去），构建通过但运行时 `undefined`。**改完必须 assert 目标文本已存在。**
2. **改 UI 位置会打断冒烟测试的点击坐标**。按钮从 y=320 移到 y=386 后，测试立刻报 `状态=战斗中 FAIL`。这是特性不是缺陷 —— 但记得同步更新坐标。
3. **自动发布验证使用 Canvas 桩 + 专项探针**；另已在真实 Chromium 浏览器完成桌面、竖屏与打包版的视觉/交互验收。**真机音频输出、触控延迟、safe-area 与浏览器 chrome 仍必须在物理设备上验证。**
4. 打包版必须单独冒烟与浏览器验收。`packed-smoke` 会验证 Roadroller 自解压、主循环、精确主题文案与输入路径；真实浏览器另行验证打包产物的可见画面和交互。

## 4.2 设计与数值（每一条都曾造成严重问题）

5. **打击停顿必须是稀有事件**。第一版写在逐个敌人的伤害结算里，一次横扫砍中 8 只怪就叠 8 次停顿 → **58% 的帧在慢放**。现在只有暴击/突进斩/首领击杀/PRISM BREAK 启动才停顿（2.7%）。
6. **无敌时长绝不能随目标数缩放**。闪现无敌曾是 `0.1 + 目标数×0.075`，Phantom Chain 叠满 13 目标 = 每次 1.44 秒无敌，而冷却只有 0.9 秒 → **94.9% 的帧无敌，永远不死**。现在固定 0.35 秒，且突进总时长固定 0.3 秒（目标越多闪得越快，不是无敌越久）。
7. **击杀降冷却会归零**。Momentum 卡每次击杀砍 0.4 秒冷却，割草场景每秒杀几十只 → 冷却恒为 0。改成充能制 + 硬锁，击杀只加速充能且封顶。
8. **吸血必须封顶**。伤害按人头结算、回血也按人头结算，一刀 8 只怪就回 8 次血 → Lv30 时每秒回 560 血。改成每秒 6% 最大生命的预算制。
9. **"跑"必须失效**。后期玩家比所有怪快时，最优解永远是直线逃跑。两个修法缺一不可：**怪速上限抬到 2.7 倍** + **刷怪偏向玩家前进方向**。
10. **密度与压力曲线是两个独立旋钮**。一起拧会翻车：开局改成满屏（密度）时同时左移了血量曲线，中位存活从 90 秒掉到 55 秒。**密度决定屏幕多满，血量/伤害成长决定你能撑多久。**
11. **怪多 = 经验多 = 你更强**，这是幸存者类的正反馈。只加怪不掐经济，难度不会上升（第一次加难度后机器人反而打到 Lv93）。
12. **打不死的怪不叫虐心叫劝退**。首领曾在 75 秒登场、2500 血、玩家 60 dps —— 数学上要砍 58 秒，而它 4 下拍死你。
13. **障碍墙是负资产**。9% 密度的实心墙让中位存活从 182 秒掉到 56 秒，但那不是难度，是**大家都被卡住**。已整体移除（连碰撞函数一起删）。
14. **内容不能被时间锁死**。曾出现"中位 80 秒死，而引爆体 96 秒才登场"—— 一半图鉴玩家永远见不到。怪种解锁已全部压进前 50 秒。

## 4.3 测量方法论（**最重要的一条**）

15. **机器人是测量工具，工具本身会骗你**。
    - 第一版机器人只会直线逃离最近的怪 + 无脑突进 → 用它调平衡等于瞎测
    - 第二版教会绕圈，结果它躲得太完美，12 局全是个位数击杀 —— 在跑马拉松不是打游戏
    - 第三版让它**贴着攻击距离边缘绕圈**才有意义
    - 加地刺后它踩着刺不挪窝，正常打法测出 14 秒 —— 假的，得先**教机器人躲刺**
16. **只测"好好打"是不够的**。用户实测"无限点点根本死不了"时，我的机器人报告中位 130 秒 —— 因为**测试跑的赛道和玩家玩的不是同一条**。现在有 `cheese.mjs`：四个专门作弊的机器人，各堆一套苟活组合。**改任何数值后必跑。**
17. **随机性会让 bug 无法复现**。曾出现一次崩溃、重跑 36 次不复现 —— 因为游戏用 `Math.random()` 播种。现在测试沙箱内用 LCG，`seed-sweep.mjs` 可扫种子。

---

# 第五部分 · 待办（按优先级）

## P0 — 阻塞提交

Unicorns and Rainbows 已按批准方案落地：UNICORN 彩虹能源 AI 腐化机械为棱镜兽，七色碎片驱动 PRISM BREAK，BLACK UNICORN 是最终威胁。主题标题、目标文案、敌人视觉和彩虹能量反馈已纳入打包验证。

1. **建 GitHub 公开仓库**：这是待完成的外部发布动作；推送可读源码、构建脚本、测试和文档
2. **真机验收**：Chrome / Firefox / 手机各跑一次，**控制台不得有红色报错**；确认手机音频实际出声、触控延迟可接受，safe-area / 浏览器 chrome 下无遮挡，竖屏重排和轻点技能正常
3. **提交**：这是待完成的外部表单动作；表单开放后照 `提交清单.md` 填，**Desktop 与 Mobile 两个分类都勾**，留出截止日前缓冲

## P1 — 提分

6. 作品页第一张 GIF（评委三秒决定点不点开）
7. 提交后带 `#js13k` 发帖、去玩别人的作品并认真留评论、写 devlog

## P2 — 若还有字节

8. 待人工实测反馈后的手感微调（地刺预警提前量、PRISM BREAK 按钮误触、自适应画质触发阈值）

---

# 第六部分 · 给接手者的提醒

- **每次改动后跑 `sh tools/qa.sh`**（已包含 `cheese.mjs`），发布前再跑 `npm run verify`
- **主题版目标 ≤ 13,100 字节**，最终大小以 `build.py` 最新输出为准。加任何内容前都要重新构建并跑完整发布验证
- **不要试图靠加内容提升 Innovation 分**。这个品类的天花板在选品阶段就定了；剩下的时间投在真机打磨和提交页呈现上回报更高
- **视觉与手感的最终判断权在人类**，不在测试。任何"我觉得应该没问题"的结论都要落到真机上验
